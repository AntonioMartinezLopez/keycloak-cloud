apiVersion: v1
kind: Pod
metadata:
  name: secondapp
  labels:
    example: second
spec:
#   securityContext:
#   runAsUser: 1000
  containers:
  - name: busy 
    image: busybox
    command:
      - sleep
      - "36000"
